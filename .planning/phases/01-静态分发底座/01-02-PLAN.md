---
phase: 01-静态分发底座
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - wrangler.toml
  - .github/workflows/deploy.yml
  - scripts/upload-r2.sh
  - docs/cloudflare-static-boundary.md
  - tests/cloudflare-boundary.test.js
  - tests/build-release.test.js
autonomous: true
requirements: [PLAT-02, PLAT-01]
user_setup:
  - 在 Cloudflare 中预先准备 Pages 项目、R2 公开域名和 GitHub Actions secrets：`CLOUDFLARE_API_TOKEN`、`CLOUDFLARE_ACCOUNT_ID`
  - 为 Worker 准备真实的 `MEMBERSHIP_KV` namespace id，不能继续使用 `placeholder-kv-id`
must_haves:
  truths:
    - 公开页面与公开 JSON/m3u8 的主读取路径不再经 Worker 托管
    - 同一条 CI 流水线能用同一个 `RELEASE_ID` 构建并发布 Pages 与 R2
    - R2 上传阶段会按资源类型写入明确的 `Cache-Control`
  artifacts:
    - wrangler.toml
    - .github/workflows/deploy.yml
    - scripts/upload-r2.sh
    - docs/cloudflare-static-boundary.md
  key_links:
    - `wrangler.toml` 不再用 `[site]` 托管 `dist/`
    - `.github/workflows/deploy.yml` 调用 `build:release` 与 `upload:r2`
    - `scripts/upload-r2.sh` 根据路径/扩展名输出稳定缓存头
---

<objective>
把仓库的发布边界真正收拢到 `Pages + R2 + 极薄 Worker`：公开读流量走静态分发，CI 使用统一 `RELEASE_ID` 完成同版发布。

Purpose: 这是 Phase 1 的成本地基。只要公开访问还经过 Worker 或发布链仍然分裂，10k+ DAU 下的成本和一致性都会先失控。
Output: 去站点化的 Worker 配置、manifest 驱动的 R2 上传、同版 GitHub Actions 流水线，以及边界文档。
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-静态分发底座/01-RESEARCH.md
@.planning/phases/01-静态分发底座/01-01-PLAN.md
@.github/workflows/deploy.yml
@wrangler.toml
@scripts/upload-r2.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: 把 Worker 从公开站点托管链中移除并固化边界文档</name>
  <files>wrangler.toml, docs/cloudflare-static-boundary.md</files>
  <read_first>wrangler.toml, src/worker/index.js, .planning/phases/01-静态分发底座/01-RESEARCH.md, .planning/codebase/CONCERNS.md</read_first>
  <action>更新 `wrangler.toml`，移除 `[site] bucket = "./dist"`，保留 Worker 入口 `src/worker/index.js` 与 `MEMBERSHIP_KV` 绑定，但不再让 Worker 托管静态站点壳。新增 `docs/cloudflare-static-boundary.md`，明确记录 3 条边界：`Pages` 只服务 `/`、`/index.html`、`/css/*`、`/js/*`、`/manifest.json`、`/release.json`；`R2` 只服务 `/releases/<releaseId>/data/*` 与 `/releases/<releaseId>/m3u8/*`；`Worker` 只服务 `/api/*`。文档里要直接写明“禁止把 `/data/*`、`/m3u8/*`、首页、分类页或详情页重新代理到 Worker”。</action>
  <verify>node --test tests/cloudflare-boundary.test.js</verify>
  <acceptance_criteria>
    - `wrangler.toml` does not contain `[site]`
    - `docs/cloudflare-static-boundary.md` contains `Pages`
    - `docs/cloudflare-static-boundary.md` contains `R2`
    - `docs/cloudflare-static-boundary.md` contains `Worker`
    - `docs/cloudflare-static-boundary.md` contains `禁止把 \`/data/*\``
  </acceptance_criteria>
  <done>Worker 不再是公开站点托管入口，边界规则被写成仓库内文档。</done>
</task>

<task type="auto">
  <name>Task 2: 把 R2 上传改成 release 前缀与缓存头驱动</name>
  <files>scripts/upload-r2.sh, tests/cloudflare-boundary.test.js</files>
  <read_first>scripts/upload-r2.sh, .planning/phases/01-静态分发底座/01-RESEARCH.md, .planning/phases/01-静态分发底座/01-01-PLAN.md</read_first>
  <action>重写 `scripts/upload-r2.sh`，让它强制读取 `RELEASE_ID`，上传目标固定为 `releases/$RELEASE_ID/data/*` 与 `releases/$RELEASE_ID/m3u8/*`，不能继续平铺到固定根路径。脚本要支持 `--dry-run`，用于只打印即将上传的 key、content-type 与 cache-control，不真正调用 `wrangler r2 object put`。缓存头按扩展名写死：`.json` 使用 `public, max-age=300, s-maxage=3600, stale-while-revalidate=300`；`.m3u8` 使用 `public, max-age=60, s-maxage=600, stale-while-revalidate=60`；`.key` 使用 `public, max-age=86400, s-maxage=86400`。同时保留正确的 `Content-Type`，并在日志中打印当前 `RELEASE_ID` 与上传前缀。</action>
  <verify>node --test tests/cloudflare-boundary.test.js</verify>
  <acceptance_criteria>
    - `scripts/upload-r2.sh` contains `releases/$RELEASE_ID/data`
    - `scripts/upload-r2.sh` contains `releases/$RELEASE_ID/m3u8`
    - `scripts/upload-r2.sh` contains `--dry-run`
    - `scripts/upload-r2.sh` contains `max-age=300`
    - `scripts/upload-r2.sh` contains `max-age=60`
    - `scripts/upload-r2.sh` contains `max-age=86400`
  </acceptance_criteria>
  <done>R2 上传具备 release 前缀和明确缓存头，不再依赖手工约定。</done>
</task>

<task type="auto">
  <name>Task 3: 把 GitHub Actions 收敛成同版发布流水线</name>
  <files>.github/workflows/deploy.yml, tests/build-release.test.js, tests/cloudflare-boundary.test.js</files>
  <read_first>.github/workflows/deploy.yml, package.json, scripts/build-release.js, scripts/upload-r2.sh</read_first>
  <action>更新 `.github/workflows/deploy.yml`，让流水线在同一次执行里生成一个 `RELEASE_ID=${GITHUB_SHA::12}`，然后依次执行 `npm ci`、`npm run build:release`、`npm run upload:r2` 和 `wrangler pages deploy dist/ --project-name=jiuyao`。如果 Worker 仍需单独部署，必须把它作为低频附属步骤，且不能让 Pages 与 Worker 都托管同一个 `dist/`。工作流日志中要显式输出当前 `RELEASE_ID`，并让上传脚本与 Pages 部署都消费这个值。</action>
  <verify>node --test tests/cloudflare-boundary.test.js tests/build-release.test.js</verify>
  <acceptance_criteria>
    - `.github/workflows/deploy.yml` contains `RELEASE_ID`
    - `.github/workflows/deploy.yml` contains `npm ci`
    - `.github/workflows/deploy.yml` contains `npm run build:release`
    - `.github/workflows/deploy.yml` contains `npm run upload:r2`
    - `.github/workflows/deploy.yml` contains `pages deploy dist/`
  </acceptance_criteria>
  <done>CI 具备同版发布 Pages 与 R2 的基础能力。</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm test`
- [ ] `node --test tests/cloudflare-boundary.test.js tests/build-release.test.js`
- [ ] `rg -n "\[site\]" wrangler.toml` 无输出
- [ ] `RELEASE_ID=test-phase1 bash scripts/upload-r2.sh --dry-run` 能打印 release 前缀与缓存头策略
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- `wrangler.toml` 不再把公开站点壳托管给 Worker
- CI 使用单一 `RELEASE_ID` 同步构建并发布前端与数据
- R2 上传脚本按资源类型写入缓存头
  </success_criteria>

<output>
After completion, create `.planning/phases/01-静态分发底座/01-02-SUMMARY.md`
</output>
