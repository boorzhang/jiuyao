---
phase: 01-静态分发底座
plan: 03
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - src/frontend/index.html
  - src/frontend/js/app.js
  - src/frontend/js/api.js
  - src/frontend/sw.js
  - src/frontend/vendor/hls.min.js
  - src/frontend/_headers
  - tests/cache-policy.test.js
  - tests/frontend-release.test.js
autonomous: true
requirements: [PLAT-03, PLAT-02]
user_setup: []
must_haves:
  truths:
    - 前端启动时会感知当前 `releaseId`，不会无限沿用旧缓存
    - HTML、`release.json`、JS/CSS、JSON、m3u8 具有清晰且分层的缓存合同
    - 播放器依赖固定在仓库内，不再运行时漂移
  artifacts:
    - src/frontend/_headers
    - src/frontend/vendor/hls.min.js
    - src/frontend/sw.js
    - tests/cache-policy.test.js
    - tests/frontend-release.test.js
  key_links:
    - `src/frontend/js/app.js` 读取 `release.json` 并把 `dataBase` 传给 `api.js`
    - `src/frontend/sw.js` 的缓存名包含 `releaseId`
    - `src/frontend/index.html` 不再引用 `hls.js@latest`
---

<objective>
让浏览器运行时真正消费 Phase 1 的发布合同：固定播放器版本、按 `releaseId` 切换数据基址，并为 Pages 与 Service Worker 建立明确缓存边界。

Purpose: 只有部署链和浏览器运行时都理解同一套版本/缓存规则，Phase 1 才算完成；否则 CI 再完整，用户端仍可能继续命中旧资源。
Output: release-aware 前端引导、固定播放器文件、Pages `_headers`、SW 缓存策略与对应测试。
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-静态分发底座/01-RESEARCH.md
@.planning/phases/01-静态分发底座/01-01-PLAN.md
@src/frontend/index.html
@src/frontend/js/api.js
@src/frontend/sw.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: 让前端按 release manifest 启动，并固定播放器依赖</name>
  <files>src/frontend/index.html, src/frontend/js/app.js, src/frontend/js/api.js, src/frontend/vendor/hls.min.js</files>
  <read_first>src/frontend/index.html, src/frontend/js/app.js, src/frontend/js/api.js, .planning/phases/01-静态分发底座/01-RESEARCH.md</read_first>
  <action>把 `src/frontend/index.html` 中的 `https://cdn.jsdelivr.net/npm/hls.js@latest` 替换为仓库内静态文件 `/vendor/hls.min.js`，并把该文件提交到 `src/frontend/vendor/hls.min.js`。更新 `src/frontend/js/app.js`，在页面初始化前先读取 `/release.json`，再把 `dataBase` 或 `r2Base` 交给 `api.js`。更新 `src/frontend/js/api.js`，使其在 release 切换时清空内存 JSON 缓存，并始终基于 manifest 提供的数据前缀构造 `/data/*` 与 `/m3u8/*` 请求地址，不能继续只靠 `location.hostname === 'localhost'` 推断生产路径。</action>
  <verify>node --test tests/frontend-release.test.js</verify>
  <acceptance_criteria>
    - `src/frontend/index.html` does not contain `hls.js@latest`
    - `src/frontend/index.html` contains `/vendor/hls.min.js`
    - `src/frontend/js/app.js` contains `release.json`
    - `src/frontend/js/api.js` contains `clearCache`
    - `src/frontend/js/api.js` contains `setR2Base`
  </acceptance_criteria>
  <done>浏览器运行时能够理解当前 release，并停止依赖漂移的 CDN 播放器脚本。</done>
</task>

<task type="auto">
  <name>Task 2: 为 Pages 和 Service Worker 建立明确缓存合同</name>
  <files>src/frontend/_headers, src/frontend/sw.js, src/frontend/js/api.js</files>
  <read_first>src/frontend/sw.js, src/frontend/js/api.js, .planning/phases/01-静态分发底座/01-RESEARCH.md, scripts/upload-r2.sh</read_first>
  <action>新增 `src/frontend/_headers`，写入固定规则：`/`、`/index.html`、`/release.json` 为 `Cache-Control: no-store`；`/assets/*` 为 `Cache-Control: public, max-age=31536000, immutable`。更新 `src/frontend/sw.js`，让缓存名包含当前 `releaseId`，并保持 `m3u8` 为 network-only、JSON 为 network-first + fallback、站点壳为 cache-first，但在 release 切换时清理旧缓存。若 `api.js` 内存在图片和 JSON 缓存，也要在 release 切换时显式清空，避免旧版本内存缓存继续存活。</action>
  <verify>node --test tests/cache-policy.test.js</verify>
  <acceptance_criteria>
    - `src/frontend/_headers` contains `/release.json`
    - `src/frontend/_headers` contains `/assets/*`
    - `src/frontend/_headers` contains `Cache-Control: no-store`
    - `src/frontend/_headers` contains `immutable`
    - `src/frontend/sw.js` contains `releaseId`
    - `src/frontend/sw.js` contains `network only`
  </acceptance_criteria>
  <done>浏览器端和 Pages 端都具备可解释的缓存分层。</done>
</task>

<task type="auto">
  <name>Task 3: 为前端 release 与缓存策略补充回归测试</name>
  <files>tests/cache-policy.test.js, tests/frontend-release.test.js</files>
  <read_first>tests/feeds.test.js, tests/categories.test.js, src/frontend/js/api.js, src/frontend/sw.js, src/frontend/_headers</read_first>
  <action>新增 `tests/frontend-release.test.js`，验证前端初始化逻辑会先读取 `release.json`，并把 manifest 中的数据基址传给 API 层。新增 `tests/cache-policy.test.js`，验证 `_headers` 中 HTML 与 `release.json` 使用 `no-store`，并验证 `src/frontend/sw.js` 仍对 `.m3u8` 采用 network-only、对 JSON 使用 network-first。测试必须检查明确字符串，而不是只断言文件存在。</action>
  <verify>npm test && node --test tests/cache-policy.test.js tests/frontend-release.test.js</verify>
  <acceptance_criteria>
    - `tests/frontend-release.test.js` contains `release.json`
    - `tests/cache-policy.test.js` contains `no-store`
    - `tests/cache-policy.test.js` contains `.m3u8`
    - `tests/cache-policy.test.js` contains `network-first`
  </acceptance_criteria>
  <done>Phase 1 的缓存合同被自动化测试固定下来。</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm test`
- [ ] `node --test tests/cache-policy.test.js tests/frontend-release.test.js`
- [ ] `rg -n "hls.js@latest" src/frontend/index.html` 无输出
- [ ] `rg -n "Cache-Control: no-store|immutable" src/frontend/_headers`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- 前端启动先读取 release 指针，再决定当前数据源
- HTML / release 指针 / 静态资产 / JSON / m3u8 的缓存边界清晰可查
- 播放器依赖固定在仓库内
  </success_criteria>

<output>
After completion, create `.planning/phases/01-静态分发底座/01-03-SUMMARY.md`
</output>
