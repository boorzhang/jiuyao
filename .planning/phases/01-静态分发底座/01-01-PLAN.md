---
phase: 01-静态分发底座
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - scripts/build-release.js
  - scripts/lib/release.js
  - scripts/build-frontend.js
  - src/build/index.js
  - src/build/config.js
  - tests/release-manifest.test.js
  - tests/build-release.test.js
autonomous: true
requirements: [PLAT-02]
user_setup: []
must_haves:
  truths:
    - 仓库存在单一 `RELEASE_ID` 来源，前端与数据构建共享同一版本号
    - `npm ci` 可在干净环境中成功执行
    - `dist/release.json` 与 `r2-data/data/config.json` 同时暴露相同的 `releaseId`
  artifacts:
    - package-lock.json
    - scripts/lib/release.js
    - scripts/build-release.js
    - dist/release.json
    - r2-data/data/config.json
  key_links:
    - `scripts/build-release.js` 同时调用前端构建与数据构建，并向两侧传递同一个 `RELEASE_ID`
    - `src/build/config.js` 输出的 `releaseId` 与 `scripts/lib/release.js` 解析出的值一致
---

<objective>
建立 Phase 1 的发布版本基线，让前端壳、R2 数据和后续部署脚本共享同一 `releaseId`，并修复当前 `npm ci` 无法执行的问题。

Purpose: 只有先把版本号、构建入口和锁文件固化，后面的 Cloudflare 发布边界与缓存策略才有可验证的共同基础。
Output: shared release 工具、统一 build 脚本、锁文件，以及覆盖 release 产物的测试。
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-静态分发底座/01-RESEARCH.md
@package.json
@scripts/build-frontend.js
@src/build/index.js
@src/build/config.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: 建立共享 release manifest 工具与数据版本字段</name>
  <files>scripts/lib/release.js, src/build/index.js, src/build/config.js</files>
  <read_first>scripts/build-frontend.js, src/build/index.js, src/build/config.js, .planning/phases/01-静态分发底座/01-RESEARCH.md</read_first>
  <action>新增 `scripts/lib/release.js`（ESM）作为全仓库唯一 release 入口，至少导出 `resolveReleaseId()`, `buildReleaseManifest()`, `writeJsonFile()` 三个函数。`resolveReleaseId()` 的优先级固定为 `process.env.RELEASE_ID` → `process.env.GITHUB_SHA` 的前 12 位 → UTC 时间戳；不要继续使用 `new Date().toISOString().slice(0, 10)` 作为唯一版本号。更新 `src/build/index.js`，让数据构建在读取视频与写出 `r2-data/` 时把 `releaseId` 与 `generatedAt` 透传给 `src/build/config.js`。更新 `src/build/config.js`，让 `r2-data/data/config.json` 至少包含 `releaseId`、`generatedAt`、`r2Base`、`categories`、`feeds`、`totalVideos`，并保留原有 `cdnBase`。</action>
  <verify>node --test tests/release-manifest.test.js</verify>
  <acceptance_criteria>
    - `scripts/lib/release.js` contains `export function resolveReleaseId(`
    - `scripts/lib/release.js` contains `export function buildReleaseManifest(`
    - `src/build/config.js` contains `releaseId`
    - `src/build/config.js` contains `generatedAt`
  </acceptance_criteria>
  <done>共享 release 工具落地，数据构建不再依赖日期字符串冒充版本号。</done>
</task>

<task type="auto">
  <name>Task 2: 增加统一 build:release 入口并提交锁文件</name>
  <files>package.json, package-lock.json, scripts/build-release.js, scripts/build-frontend.js</files>
  <read_first>package.json, scripts/build-frontend.js, src/build/index.js, .github/workflows/deploy.yml</read_first>
  <action>新增 `scripts/build-release.js`，作为 Phase 1 的统一构建入口。该脚本要先解析同一个 `RELEASE_ID`，再顺序调用前端构建和数据构建，并在 `dist/release.json` 写出 `releaseId`、`generatedAt`、`dataBase`、`apiBase`、`assetPrefix`。同时升级 `scripts/build-frontend.js`，让它把 `css/`、`js/`、`vendor/` 输出到 `dist/assets/<releaseId>/...`，并重写 `dist/index.html` 中对 CSS、JS 和播放器脚本的引用，确保这些前端资产从一开始就是 release 作用域路径，后续才能安全使用 `immutable` 缓存。更新 `package.json` 脚本，至少新增 `build:release`，并让默认 `test` 能覆盖 `tests/*.test.js` 与 `tests/*.test.cjs`。生成并提交 `package-lock.json`，保证 `.github/workflows/deploy.yml` 里的 `npm ci` 在干净环境可执行。</action>
  <verify>node --test tests/build-release.test.js</verify>
  <acceptance_criteria>
    - `package.json` contains `"build:release"`
    - `package.json` contains `tests/*.test.cjs`
    - `package-lock.json` exists
    - `scripts/build-release.js` contains `dist/release.json`
    - `scripts/build-release.js` contains `assetPrefix`
    - `scripts/build-frontend.js` contains `dist/assets`
  </acceptance_criteria>
  <done>仓库具备统一 build 入口，CI 不再因缺少锁文件直接失败。</done>
</task>

<task type="auto">
  <name>Task 3: 为 release 构建基线补充自动化回归</name>
  <files>tests/release-manifest.test.js, tests/build-release.test.js</files>
  <read_first>tests/categories.test.js, tests/feeds.test.js, tests/recommend.test.js, scripts/lib/release.js, scripts/build-release.js</read_first>
  <action>新增 `tests/release-manifest.test.js`，覆盖 `resolveReleaseId()` 的优先级、manifest 字段完整性和 fallback 行为。新增 `tests/build-release.test.js`，使用临时目录和小型 fixture 验证 `build:release` 会同时写出 `dist/release.json` 与 `r2-data/data/config.json`，且两者 `releaseId` 完全相同。测试风格继续使用 Node 原生 `node:test` 与真实文件系统，不引入额外框架。</action>
  <verify>npm test</verify>
  <acceptance_criteria>
    - `tests/release-manifest.test.js` contains `resolveReleaseId`
    - `tests/build-release.test.js` contains `dist/release.json`
    - `tests/build-release.test.js` contains `config.json`
  </acceptance_criteria>
  <done>后续所有发布相关改动都有快速回归护栏。</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm test`
- [ ] `node --test tests/release-manifest.test.js tests/build-release.test.js`
- [ ] `RELEASE_ID=test-phase1 node scripts/build-release.js` 成功生成 `dist/release.json` 与 `r2-data/data/config.json`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- 仓库存在单一 release 解析入口
- `npm ci` 不再因锁文件缺失失败
- 前端和数据构建共享同一个 `releaseId`
  </success_criteria>

<output>
After completion, create `.planning/phases/01-静态分发底座/01-01-SUMMARY.md`
</output>
